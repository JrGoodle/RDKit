#!/usr/bin/env bash

set -euo pipefail
cd "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/.."

# rm -rf \
#     rdkit-*.tar.gz \
#     rdkit/build \
#     rdkit/stage \
#     rdkit/lib \
#     rdkit/frameworks
